{
	"name": "allowlist-action",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc -noEmit && yarn esbuild index.ts --bundle --outdir=dist --out-extension:.js=.cjs --platform=node --target=node20.0.0 --packages=bundle",
		"check": "biome check .",
		"check-fix": "biome check --write ."
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@actions/core": "^3.0.1",
		"@actions/github": "^9.1.1",
		"@octokit/plugin-throttling": "^11.0.3",
		"ajv": "^8.20.0",
		"octokit": "^5.0.5",
		"yaml": "^2.8.3"
	},
	"type": "module",
	"packageManager": "yarn@4.9.1",
	"devDependencies": {
		"@biomejs/biome": "2.4.13",
		"@types/node": "^25.5.1",
		"esbuild": "0.28.0",
		"typescript": "^6.0.3"
	},
	"engines": {
		"node": ">=24"
	}
}
